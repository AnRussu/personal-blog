alembic==1.14.0
altgraph==0.17.4
auto-py-to-exe==2.41.0
blinker==1.8.2
bottle==0.12.25
bottle-websocket==0.2.9
certifi==2024.2.2
cffi==1.16.0
chardet==5.2.0
Flask==2.3.2  # Основний веб-фреймворк для побудови сайту
Jinja2==3.1.2  # Шаблони для відображення HTML-сторінок
Werkzeug==2.3.2  # Інструменти для обробки запитів та сесій
itsdangerous==2.1.2  # Для захисту секретних ключів у Flask
click==8.1.3  # Для консольних команд у Flask

# Додаткові бібліотеки
sqlite3  # База даних SQLite (вбудована, не потребує встановлення)

# Для роботи з медіафайлами
Pillow==9.3.0  # Обробка зображень, якщо плануєте додавати їх обробку
